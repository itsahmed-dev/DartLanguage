void main() {
  // Calling the funtion
  var result = add(5, 10);
  // printing the output
  print(result);
}

int add(int a, int b) {
  // Main job of the funtion
  var calculation = a + b;
  // Returning the output
  return calculation;
}
