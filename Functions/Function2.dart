void main() {
  // Calling the function
  var result = add(5, 10);
  // printing the output
  print(result);
}

int add(int a, int b) {
  // Main job of the function
  var calculation = a + b;
  // Returning the output
  return calculation;
}
