// Dart - Collection: Set

// Set Operations

void main() {
  Set colors = {'Blue', 'Yellow', 'Orange'};
  Set fruits = {'Apple', 'Mango', 'Orange'};
  print(colors.union(fruits)); // Union Operation
  print(colors.intersection(fruits)); // Intersection Operation
  print(colors.difference(fruits)); // Difference Operation
}
